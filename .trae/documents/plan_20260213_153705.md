I have analyzed the code in `app/resume/page.jsx` and while the static analysis tools don't show a syntax error, the "Invalid or unexpected token" error combined with your selection of `const skills = {` suggests there might be a hidden character or a malformed closure in the preceding `education` object.

I noticed that the `education` object in the current file has changed (it now has 2 items instead of the original 6), suggesting recent edits might have introduced the issue.

My plan is to:

1. Completely rewrite the `education` object and the declaration of `skills`

